/* ==========================================================================
   Trang Quét Bài Báo (/admin/blogs/scraper) - Server Component
   ========================================================================== */

import ScraperClient from './ScraperClient';

export default function AdminScraperPage() {
  return <ScraperClient />;
}
